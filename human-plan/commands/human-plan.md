---
description: Write an implementation plan a human can meaningfully review and approve
model: opus
---

Invoke the `plan-for-humans` skill and follow it end to end: Phase 0 elicitation, draft against `references/template.md`, `plan-critic` pass, revise, present, then walk the user through the decision register and open questions one entry at a time.

Start at Phase 0 unless the user's message below already settles the requirements. Ask one question at a time.

Planning subject: $ARGUMENTS
